# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ค่าจริง (ห้าม commit .env.local)

# LINE Developers Console > Messaging API channel
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# Google AI Studio API key (ใช้กับ @google/genai)
GEMINI_API_KEY=

# Google Sheet > File > Share > Publish to web > CSV
SHEET_CSV_URL=
